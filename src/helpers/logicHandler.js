import React from 'react';
import { connect } from 'tls';

const LogicHandler = () => {

}

const mapStateToProps = state => {
    return {
        
    }
}

export default connect(mapStateToProps)(LogicHandler);